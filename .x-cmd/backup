
git clone git@github.com:x-cmd/yanfa
cd yanfa
xws xx x0 | {
    x job failfast init
    while read -r repo; do
        # x job failfast put x gitb backup "git@gitee.com:x-bash/$repo" "git@github.com:x-bash/$repo"
        x job failfast put x gitb backup "git@github.com:x-bash/$repo" "git@gitee.com:x-bash/$repo"
        # TODO: codeup
    done | (
        x job failfast fini
    )
}

# xws xx x0 ｜ x job failfast run
# x job failfast run -- xws xx x0

# xws xx x0 | {
#     while read -r repo; do
#         x gitb sync "git@gitee.com:x-bash/$repo" "git@github.com:x-bash/$repo"
#     done
# }
